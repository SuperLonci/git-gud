package main

import (
	"fmt"
)

func main() {
	fmt.Println(`
     _~^~^~_
\) /  o o \ (/
  '_   ¬   _'
  / '-----' \     GIT GUD

Your friendly Git help command:

  git status         Show working tree status
  git add <file>     Add to staging
  git commit -m ""   Save your crimes
  git push           Ship it
  git pull           Get new regrets
  git log            See past regrets

Now stop asking for help and GIT GUD.
`)
}
